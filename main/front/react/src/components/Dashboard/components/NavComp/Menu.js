import classes from "./Menu.module.css";

const Menu = () => {
  return (
    <div className={classes.container}>
      <div className={classes.card}>Dashboard</div>
    </div>
  );
};

export default Menu;
